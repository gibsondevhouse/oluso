<script lang="ts">import { CircleAlert } from "lucide-svelte"; interface Props { title?: string; message: string; onRetry?: () => void; } let { title = "This workspace could not be loaded", message, onRetry }: Props = $props();</script>
<section class="error-state" role="alert"><CircleAlert size={24} /><div><h2>{title}</h2><p>{message}</p></div>{#if onRetry}<button type="button" onclick={onRetry}>Try again</button>{/if}</section>
<style>.error-state { display: grid; grid-template-columns: auto 1fr auto; align-items: start; gap: 12px; border: 1px solid var(--color-danger-border); border-radius: var(--radius-surface); background: var(--color-danger-soft); color: var(--color-danger); padding: 16px; } h2,p{margin:0} h2{font-size:.9rem}p{font-size:.8rem}button{min-height:34px;border:1px solid var(--color-danger-border);border-radius:var(--radius-control);background:var(--color-surface);color:var(--color-danger);font-weight:700;padding:0 10px}</style>
