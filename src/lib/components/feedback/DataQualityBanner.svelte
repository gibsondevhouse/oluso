<script lang="ts">import { BadgeAlert, CheckCircle2 } from "lucide-svelte"; interface Props { state: "verified" | "needs-review" | "unknown"; message: string; href?: string; } let { state, message, href }: Props = $props(); const Icon = $derived(state === "verified" ? CheckCircle2 : BadgeAlert);</script>
<div class="data-quality-banner" class:verified={state === "verified"} role="status"><Icon size={17}/><div><strong>{state === "verified" ? "Data verified" : state === "needs-review" ? "Data needs verification" : "Data quality unknown"}</strong><span>{message}</span></div>{#if href}<a {href}>Review</a>{/if}</div>
<style>.data-quality-banner{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:10px;border:1px solid var(--color-warning-border);border-radius:var(--radius-surface);background:var(--color-warning-soft);color:var(--color-warning-text);padding:11px 13px}.verified{border-color:var(--color-positive-border);background:var(--color-positive-soft);color:var(--color-positive-text)}div div{display:grid;gap:1px}strong{font-size:.8rem}span{color:var(--color-muted);font-size:.75rem}a{font-size:.75rem;font-weight:700}</style>
